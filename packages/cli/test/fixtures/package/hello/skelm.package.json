{
  "name": "@skelm/hello",
  "version": "0.1.0",
  "description": "Greets someone by name.",
  "license": "MIT",
  "skelm": {
    "apiVersion": 1,
    "requiredSkelmVersion": ">=0.4.0",
    "workflows": [
      {
        "id": "default",
        "entry": "workflows/hello.workflow.ts",
        "kind": "pipeline",
        "description": "Greets someone by name."
      }
    ]
  }
}
