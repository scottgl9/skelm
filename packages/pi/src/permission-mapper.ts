// Pi backend uses toolPermissions: 'native' — the runner passes
// the resolved policy to the agent as advisory metadata only.
// Permission enforcement happens at the pi process level.
//
// This file is retained as a placeholder; the backend no longer
// calls validatePermissions directly.

export {} // keep module shape
