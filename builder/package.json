{
  "name": "skelm-builder",
  "version": "0.1.0",
  "private": true,
  "description": "A skelm workflow that builds other skelm workflows",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "true"
  },
  "dependencies": {
    "@earendil-works/pi-coding-agent": ">=0.75.0",
    "@skelm/pi": "workspace:*",
    "skelm": "workspace:*",
    "zod": "^4.4.2"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
