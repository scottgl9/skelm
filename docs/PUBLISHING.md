# Publishing

The publishing guide moved to the repo root: [`PUBLISHING.md`](../PUBLISHING.md).

It covers both npmjs.org (`skelm`, `@skelm/*`) and GitHub Packages (`@scottgl9/*`), the per-release script, and the post-publish "make GitHub Packages public" step.
